[package]
name = "isa198x"
description = "Declarative instruction-set specifications for the 198x family CPUs."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

# Zero dependencies, by design. This is the neutral layer Emu198x can depend on
# without pulling in an assembler or emulator.

[lints]
workspace = true
